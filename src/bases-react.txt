React: biblioteca javascript usada para desenvolver interfaces de usuario(UI). 
    -Componente é um dos conceitos core(centrais do react)- São a fundação da UI da aplicação. São funções JS declaradas em PascalCase, que podemos usar linguagem de marcação (html) que recebem props, que vão ter um estado e que vão ser renderizados. (em camelCase o navegador entende que é elemento html)

    -Exporte o componente no top level do módulo - crie a função - adicione logica js - adicione linguagem de marcação

React adota que a lógica de renderização é acoplada com outras lógicas de ui: manipulação de eventos, estado mudando e exibição de dados

JSX: É uma extensão de sintaxe do javascript que permite escrever html dentro do arquivo javascript. A lógica de renderização e conteúdo (ui) no mesmo lugar, interagindo.

    Regras: 
        um componente retorna um único elemento root dentro de <></>(fragmento que permite que você agrupe elementos sem deixar rastros na arvore html do navegador), ou de uma div que engloba tudo.

        atributos dos componentes em camelCase
        para usar variaveis ou js usamos atributo={} nos atributos ou dentro do componente para renderização condicional

        JSX previne ataques de injeção 

    function formatUserName(user){
        return `${user.firstName} ${user.lastName}`
    }

    const user = {
        firstName: "Momozinho",
        lastName: "Gomes"
    }

    chamando função dentro do jsx
    const element = (
        <h1> Hi, {formatUserName(user)} </h1>
    )

Componentes permitem que a interface do usuario seja divida em partes independentes e reutilizaveis. Conceitualmente, os componentes são funções js, permitem entradas arbitrárias (props) e retornam elementos react que devem ser renderizados.

Props:
    Argumentos passados para componentes react. Fluxo unidirecional (de cima pra baixo/ pai para filho) Props são imutaveis, os filhos não devem alterar os dados. Podem ser tipos simples, objetos, arrays e funções.

    ex: export function Avatar({person, size}) {
        <h1>Name:{person.name}</h1>
        <h1>Size:{size}</h1>
    }//person é um objeto, mas poderia ser um array, uma função...
    <Avatar person={{
        imageId: 'szV5sdG',
        name: 'Maria Skłodowska-Curie',
        profession: 'physicist and chemist',
        discovery: 'polonium (chemical element)',
        awards: [
          'Nobel Prize in Physics',
          'Nobel Prize in Chemistry',
          'Davy Medal',
          'Matteucci Medal'
        ],
      }} passando o objeto inteiro dentro da props

Renderização condicional: usado para renderizar ou não um componente a depender de uma condição. Podemos usar ternarios, && e if/else. Se uma variavel retornar null a partir de uma verifcação, o componente não érenderizado

    ex: 
    export function Button({color, disabled} : ButtonProps){
        <button color={color} disabled={disabled}>
            Send
        </button>
    }

    /status pode ser cancelado ou ativo
    export function Form({data}:FormProps){
        const status = data.status;
        const isDisabledButton? = data.isDisabled;
        {status &&
            <Button color={data.color} disable={isDisabledButton?}/>
        }
        {!status &&
            <Button color={data.color} disable={isDisabledButton?/>
        }
    }

    A documentação do react aponta que é melhor fazer a renderização condicional no pai, com {&&} ou {? () : ()}, do que passar uma props para o filho indicando se ele vai ser renderizado ou não

    Todos os componentes react tem que agir como funções puras em relaçãos aos seus props, estado e contexto, não posso alterar a entrada. 
    Componentes Recebem dados dinâmicos do componente pai para tornar ele reutilizavel. Props são imutáveis, são acessadas por desestruturação ou acessando tipo propriedades de um objeto.

Pureza: funções puras são aquelas que nçao alteram nenhum objeto ou variavel que exista antes de ser chamado. Mesmas entradas e mesmas saída. Dada as mesmas entradas, uma função pura deve retornar o mesmo resultado.

Os componentes devem apenas retornar seu jsx e não alterar nenhum objeto ou variavel que exista antes da renderização, isso os tornaria impuros. 

    let guest = 0;

    function Cup() {
    // Bad: changing a preexisting variable!
    guest = guest + 1;
    return <h2>Tea cup for guest #{guest}</h2>;
    }

    export default function TeaSet() {
    return (
        Quando chamo a função duas vezes ele muda o retorno.
        <>
        <Cup /> = 1
        <Cup /> = 2
        <Cup />= 3
        </>
    );
    }

No entanto podemos alterar variaveis e objetos que acabamos de criar durante a renderização - mutação local dentro do componente. Ou também usamos efeitos colaterais.

Efeitos colaterais geralmente estão dentro de event handlers, e eles não executam durante a renderização mesmo estando dentro do componente, então eles não precisam ser puros. Ou então usamos useEffect() se não tiver nenhum manipulador de eventos capazes de fazer o side effect.

Não alterar variaveis fora do escopo do código, passe props de pais para filhos e as retorne.

- A interface do usuario como arvore: 
assim como navegadores e plataformas mobile, o react também usa estruturas em árvore para gerenciar e modelar a relação entre os componentes da aplicação

- árvore de renderização: a renderização de componentes pais e seus componentes filhos. O componente root, App, é o primeiro componente que o react renderiza. Componentes mais próximos do raiz (root) são componentes top level e de maior complexidade, afetando o desempenho de renderização de todos os componentes filhos. Representam a relação entre os componentes em um único render.

-Para o APP ir para produção, há a etapa de compilação que agrupa todo o JS necessário para ser enviado ao cliente, por algum bundler, onde módulos representam a aplicação no browser com suas dependencias. Module bundler (webpack, cria módulos que rpresentam a aplicação).

bundlers analisam as dependencias da aplicação durante o build, criam um grafo de módulos e geram um ou mais bundles otimizados para serem carregador pelo navegador 

State: memória do componente. como o componente reage a mudanças. atualizar wa variavel e renderizar isso na tela. o hook useSate tem uma variavel pra reter os dados entre as renderizações e uma função para atualizar a variavel e acionar o reat pra renderizar o componente. Sem efeitos colaterais no setState(), apenas funções puras

  //usando useState pra monitorar o valor da variavel e renderizar a tela com o valor atualizado

  //quando precisamos do valor anterior é bom passar uma arrow function dentro do set
  const [numero, setNumero] = useState(0)
  //sempre que lido com on, a função recebe o nome de handle. usamos arrow function dentro da função para guardar o estado anterior da variavel (se eu preciso usar o valor anterior da variavel)
  function handleClick() {
    setNumero((numero) => numero +1)
  } 

    //hook usado pra efeitos colaterais, coisas que o react não monitora (tipo chamadas de API, mudar tema, salvar no local storage)
    useEffect(() => {
      console.log(Date());
    }); //uma forma de uso que sempre é executado qd o componente renderiza
    useEffect(() => {}, []);o use effect executa apenas quando o react monta o componente na tela pela primeira vez  muuto usado em consultas a API, para não ficar fazendo chamadas a cada vez que o componente renderiza
    useEffect(() => {}, [theme]); //array de dependencia quando passo um outro paramero com array [], 


localStorage é um tipo de armazenamento do navegador que salva os dados (strings) no formato chave e valor de maneira permanente. consigo setar, ler, apagar e limpar todos os dados

localstorage é separado por origin (protocolo, host, porta) = esse mesmo conjunto compartilha o mesmo localstorage

Quer persistência real → localStorage ou cookie (podem ser enviados ao servidor)

session é parecido
Quer isolamento + mais segurança → sessionStorage
Quer segurança forte → cookie httpOnly + backend session/token

let themeIcon
if (theme === 'dark'){
    themeIcon = <SunIcon />
}else{
    themeIcon = <MoonIcon />wl
}

lazy initialization = bom para perfomance. React só chama a função uma vez quando o componente é montado e ignora completamente a função nas renderizações seguintes. ex: localstorage, sessionStorage, leitores de valor do dom, criar objetos grandes

- Principio da imutabilidade
Ao trabalhar com objetos mutáveis no react (objetos e arrays(objeto)), não podemos altera-los diretamente, é uma má prática. Temos que copiar o objeto em seu estado previo e recriar o mesmo.

Quando alteramos o objeto diretamente, a referencia desse objeto permanece a mesma e o react não detecta a alteração e não re-renderiza o componente. 

Context API
Contexto é uma forma de passar dados entre a arvores de componentes sem usar props drilling (passar props manualmente entre componentes até chegar em um componente abaixo na arvore)
- usados: preferencas locais e tipos de UI (toast compartilhado por toda a aplicação)
- indicado usar contexto para comaprtilhar dados considerados globais pela arvore de componentes do react, ex: usuario autenticado, idioma preferido

-criar contexto tipado - crio provider e hook com o mesmo tipo do contexto, convenção de type safety e também por segurança

-crio um hook personalizado com o contexto que quero compartilhar por toda a aplicação(o hook deve retornar exatamente o que o provider passa de valor, por convenção do typescript), nesse hook vou ter o valor inicial e a função para atualizar o valor dela

crio contexto - tipo o contexto com state e setStae - crio hook para usar o contexto - crio componente que usa o provider do contexto e o state e set state como value, o state é o tipo initialContextState constante usada/objeto literal que contem todos os dados da apliação. ela representa o estado a ser renderizado pelos componentes o os dados que vamos passar por cada componente dentro do provider

Controlled Components:
Componentes controlados é um conceito que se aplica a componentes cujo os valors são gerenciados pelo estado do react. Os dados dele são validados e atualizados pelo estado. Isso gera muitas re-renderizações

componentes controlados são aqueles onde o react controla o valor através do state, não o DOM - usamos useState() renderizamos o componente várias vezes.
- propriedades value e onChange controlam o valor atual do input e a atualização para um novo valor - usamos useState pra validar o input em tempo real, no tempo que o usuario digita. o onChange monitora cada atualizaçao, cada letra, e dispara a função pra atualziar o valor do formulario e re-renderizar o componente.

  //controlando o estado do input para capturar o valor e renderizar o componente na tela
  const [ taskName, setTaskName ] = useState(" ");

  //função para atualizar o input, pegando o valor no target em questão que é o input
  const handleChange = (event: React.ChangeEvent<HTMLInputElement>) => {
     setTaskName(event.target.value)
   }
    value={taskName}
    onChange={handleChange}

Forms não controlado = useRef() - acessa elementos do DOM - Usado em formularios grandes quando não precisamos validar o valor em tempo real- caso de uso focus 
Cria uma referência mutavel que persiste entre renderizações sem causar re-render quando o valor muda
const numero = userefe(2)
numero.current += 2; //mudando o valor e não causa re-render
salva um valor numa variavel e não re-renderiza novamente o componente.

React hook form trabalha com inputs não controlados, ele armazena internamente o valor do input, logo o react não precisa atualizar o estado a cada letra digitada e o componente pai não re-renderiza 

- criação de função que recebe um valor numérico, verifica se for oito ou 0, se for, atribuimos 1 a variavel, se não for, somamos +1 a variavel. Passo o valor inicial, vindo de uma API por exemplo, ou de um objeto armazenado no front, para essa função. Armazeno o valor em uma constante e atribuo a contante ao valor de uma propriedade de um objeto 

Ternário

{condição ?
() : ()}

&&
{condição verdade &&
    componente}

Reducer - useReducer - hook usado para disparar uma ação e alterar o estado, entregando um novo estado
useReducer((estado e ação)=>{}, 0/estado inicial) ele recebe uma arrow function e sempre retorna um novo estado baseado na ação
    - tipos de reducer: mais simples é disparar uma string e usar um switch case para fazer uma condicional